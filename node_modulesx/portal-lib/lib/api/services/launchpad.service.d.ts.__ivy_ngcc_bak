import { HttpClient } from "@angular/common/http";
import { Observable } from "rxjs";
import { LaunchpadDTO } from "../model/launchpadDTO";
import { LaunchpadType } from "../model/launchpadType";
import { LaunchpadTileCreateDTO } from "../model/launchpadTileCreateDTO";
export interface GetAllLaunchpadsFilteredRequestParams {
    name?: string;
    portalId?: string;
    type?: LaunchpadType;
    userGroupId?: string;
    userId?: string;
    userOrganizationId?: string;
}
export declare class LaunchpadService {
    private httpClient;
    url: string;
    constructor(httpClient: HttpClient);
    getLaunchpads(criteria: GetAllLaunchpadsFilteredRequestParams): Observable<LaunchpadDTO[]>;
    addToLaunchpad(id: number, createTileDTO: LaunchpadTileCreateDTO): Observable<LaunchpadDTO>;
}
