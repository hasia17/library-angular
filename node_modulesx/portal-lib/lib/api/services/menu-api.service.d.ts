import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { PortalMenuItem } from '../model/menu-item.model';
import { MessageService } from 'primeng/api';
import * as ɵngcc0 from '@angular/core';
export declare class MenuApiService {
    private http;
    private messageService;
    private menuUrl;
    httpOptions: {
        headers: HttpHeaders;
    };
    constructor(http: HttpClient, messageService: MessageService);
    getMenuItems(portalId: String): Observable<PortalMenuItem[]>;
    /**
     * Handle Http operation that failed.
     * Let the app continue.
     * @param operation - name of the operation that failed
     * @param result - optional value to return as the observable result
     */
    private handleError;
    /** Log a HeroService message with the MessageService */
    private log;
    static ɵfac: ɵngcc0.ɵɵFactoryDef<MenuApiService, never>;
}

//# sourceMappingURL=menu-api.service.d.ts.map